{
  "name": "project-flow-viewer",
  "version": "2.0.0",
  "private": true,
  "description": "프로젝트 실행 흐름을 Mermaid로 탐색하는 AI 중립 로컬 뷰어",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check public/app.js && node --check public/viewer.config.js"
  },
  "engines": {
    "node": ">=18"
  }
}
